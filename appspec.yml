version: 0.0
Resources:
  - myLambdaFunction:
      Type: AWS::Lambda::Function
      Properties:
        Name: "${LAMBDA_FUNCTION_NAME}"
        Alias: "live"
        CurrentVersion: "1"
        TargetVersion: "1"